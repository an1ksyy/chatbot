<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Telegram Chatbot</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-[#0a0a1f] min-h-screen flex items-center justify-center">
    <div class="bg-[#1a237e] rounded-2xl shadow-lg p-8 w-full max-w-md flex flex-col items-center">
        <div class="mb-6 flex flex-col items-center">
            <div class="bg-[#600e0e] rounded-full h-20 w-20 mb-4"></div>
            <h1 class="text-3xl font-bold text-white mb-2">Telegram Login</h1>
            <p class="text-[#eeeeeeaf] text-sm">Enter your username and password to join the chat</p>
            <% if (error) { %>
                <div class="mt-2 text-red-400 text-sm font-semibold"><%= error %></div>
            <% } %>
        </div>
        <form action="/login" method="POST" class="w-full flex flex-col gap-4">
            <input type="text" name="username" placeholder="Username" required class="rounded-full px-4 py-3 bg-[#0a0a1f] text-white focus:outline-none focus:ring-2 focus:ring-[#1e88e5] text-lg" />
            <input type="password" name="password" placeholder="Password" required class="rounded-full px-4 py-3 bg-[#0a0a1f] text-white focus:outline-none focus:ring-2 focus:ring-[#1e88e5] text-lg" />
            <button type="submit" class="bg-[#1e88e5] hover:bg-[#1565c0] text-white rounded-full px-4 py-3 font-semibold text-lg transition">Login</button>
        </form>
        <div class="mt-4 text-center">
            <a href="/register" class="text-[#1e88e5] hover:underline">Don't have an account? Register</a>
        </div>
    </div>
</body>
</html>